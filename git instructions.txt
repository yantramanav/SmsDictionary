Global setup:

 Download and install Git
  git config --global user.name "Yantra Manav"
  git config --global user.email yantramanav@gmail.com
        

Next steps:

  mkdir SmsDictionary
  cd SmsDictionary
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:yantramanav/SmsDictionary.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:yantramanav/SmsDictionary.git
  git push origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


